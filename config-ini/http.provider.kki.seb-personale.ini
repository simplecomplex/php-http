; CONFIGURATION FILE HELP
; ------------------------------------------------------------------------------
; Character set, newline and editors:
; - character set: UTF-8
; - newline: \n, carriage return \r is illegal
; - editors: don't use ™MS ™Windows ™Notepad, use ™Notepad++
; Sections:
; - configuration must start with a [section]
; Names (section, key):
; - must consist of letters (lower, upper), digits, hyphen and underscore
; Illegal special keys:
; - null, yes, no, true, false, on, off, none
; Quotes:
; - double quotes; only required then leading or trailing space
; Values are single-lined:
; - use \n (single backslash) to indicate newline
; Values become string, except:
; - null|true|false: null, boolean
; - 8|0.8: integer, float
; Empty string value:
; - key =
; - key = ""
; Comments:
; - line comment begins with semicolon; inline comment not supported
; Example:
; [some_section]
; some-flat-var = some string
; someIndexedList[] = first
; someIndexedList[] = second
; someKeyedList[first] = primary
; someKeyedList[second] = secondary
; ------------------------------------------------------------------------------
; kki service: seb-personale.
[http-service_kki_seb-personale]
service_path = seb-personale
user = test
pass = test
;request_timeout = 20

; kki seb-personale endpoints.

; Endpoint: test.
[http-service_kki_seb-personale_test]
endpoint_path = test
; Cache response; allowed on endpoint or operation, not on provider nor service.
;cacheable = true;
;cacheable[ttl] = 3600
;cacheable[anybody] = true
;cacheable[refresh] = true

; Endpoint: cpr.
[http-service_kki_seb-personale_cpr]
endpoint_path = ???
; Cache response; allowed on endpoint or operation, not on provider nor service.
;cacheable = true;
;cacheable[ttl] = 3600
;cacheable[anybody] = true
;cacheable[refresh] = true

; Endpoint cpr: method GET
[http-service_kki_seb-personale_cpr_GET]
; Cache response; allowed on endpoint or operation, not on provider nor service.
;cacheable = true;
;cacheable[ttl] = 3600
;cacheable[anybody] = true
;cacheable[refresh] = true

[http-service_kki_seb-personale_email]
endpoint_path = ???
[http-service_kki_seb-personale_email_POST]

[http-service_kki_seb-personale_employment]
endpoint_path = ???
[http-service_kki_seb-personale_employment_GET]

[http-service_kki_seb-personale_employee_manager]
endpoint_path = ???
[http-service_kki_seb-personale_employee_manager_GET]

[http-service_kki_seb-personale_manager_roles]
endpoint_path = ???
[http-service_kki_seb-personale_manager_roles_GET]

;[http-service_kki_seb-personale_]
;endpoint_path = ???
;[http-service_kki_seb-personale__]
